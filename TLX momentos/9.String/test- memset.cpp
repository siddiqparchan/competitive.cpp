#include <iostream>
#include <cstring>

using namespace std;

int main(){
    /*int array[10];
    memset(array, -1, sizeof(array));
    for(short i = 0; i < 10; i++){
        cout << array[i] << endl;
    }*/

    int array[2 * 100];
    memset(array, 0, sizeof(array));
    cout << "selesai" << endl;

    //memset lumayan memakan waktu eksekusi
}